<h1> Links </h1>

<a href="/links/new">Add link</a>

<ul id="links">
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      <br>
      URL: <%= link.url %>
    </li>
  <% end %>
</ul>
